#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"
#include "kernel/param.h"

int execArg(int startPoint, char* output, char* arguments){
  int i = startPoint;
  char* buf[512];
  for(int j = 0; j < strlen(arguments); j++){
    buf[j]=
  }
  for(int startPoint = startPoint; output[i] != '\n'; i++){

  }
  return startPoint;
}

int main(int argc, char *argv)
{
  char *buf[512];
  while(read(1,buf,1)>0){

  }
  for(i=0;i < sizeof(buf),i++)

}